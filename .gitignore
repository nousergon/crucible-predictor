# Vendored private dep — staged from alpha-engine-lib sibling repo by
# deploy.sh before `docker build`. Must not be committed; real source is
# github.com/cipher813/alpha-engine-lib.
vendor/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Data — parquet cache and normalization stats (large, machine-generated)
data/cache/
data/norm_stats.json
data/constituents_cache.csv
# Local scratch caches / snapshots — large, machine-generated; never commit.
# (A `git add -A` once swept these into the public repo — keep them ignored.)
private/
.claude/
*.parquet

# Model checkpoints and training artifacts
checkpoints/
tune.db
# Locally-materialized model artifacts (real ones live in S3) — never commit.
*.pkl
*.pkl.meta.json
meta_*.txt
meta_*.txt.meta.json
meta_research_cal.json

# Local design/scratch docs (working notes; the tracked docs are OVERVIEW.md etc.)
alpha-engine-predictor-design-*.md
alpha-engine-predictor-dnn-design-*.md

# Evaluation and prediction outputs
eval_report.json

# Config (real values — gitignored; use *.sample.yaml for onboarding)
config/predictor.yaml

# Secrets and environment
.env
.env.*
!.env.example
*.pem
*.key

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
logs/

# Scratch temp files (dry-run dumps, ad-hoc JSON)
tmp*.json
tmp*/

# pytest
.pytest_cache/
.coverage
htmlcov/

# Gitleaks config (symlink to shared config)
.gitleaks.toml

.gitleaksbaseline.json
